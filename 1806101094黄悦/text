

 stack=[]
注释
1.压栈 push
 stack.append(1)
 stack.append(2)
 stack.append(3)
ctrl+d复制一行
ctrl+x删除一行
ctrl+/快速注释
2.出栈 python中出栈的操作是pop（）
 print(stack.pop())
 print(stack.pop())
 print(stack.pop())
 while len(stack) !=0:# !=(不等于)可以为is not
     print(stack.pop())

 for  i in range(1,11):
     stack.append(1)
一、文件结构
二、更新和提交
ctrl+k提交代码
ctrl+shift+k提交代码（出现问题）
ctrl+t更新代码
三、如何编程？
1.了解要解决的问题是什么。
2.确定解决问题的算法，算法分成多少步，每一个步骤是什么。
3.对于每一个步骤先用自然语言描述然后转化为Python代码。
4.在确保上一个步骤确定的前提下才开始下一个操作***

